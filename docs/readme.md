# SelwonkLib

Component library for my projects.

## Installation

This library is intended to be used as a Git submodule to allow for easy updates
as I develop it.

```sh
git submodule add git@github.com:kieranknowles1/selwonklib selwonklib
```
